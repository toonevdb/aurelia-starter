// TODO: check if this file is needed

import {FrameworkConfiguration} from 'aurelia-framework';

console.info('PASS');

export function configure(config: FrameworkConfiguration) {
  //config.globalResources([]);

  console.info('CONFIGURE PASS');
}
